# AI Character Chat Client

## Stack
- TypeScript
- React (Next.js App Router)
- Tailwind CSS

## Run
```bash
cd apps/client/web
npm install
npm run dev
```

Set API base URL if needed:
```bash
# apps/client/web/.env (local only) or .env.local
NEXT_PUBLIC_API_BASE_URL=<server-api-base-url>
```
